@model Factors.Web.Models.ViewModels.CategoryListViewModel
@{
    ViewData["Title"] = "دسته‌بندی‌ها";
}

<div class="page-header">
    <h4><i class="bi bi-folder"></i> دسته‌بندی‌ها</h4>
    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createModal">
        <i class="bi bi-plus-lg"></i> دسته‌بندی جدید
    </button>
</div>

<!-- Search -->
<form asp-action="Index" method="get" class="mb-3">
    <div class="row g-2">
        <div class="col-md-6">
            <div class="input-group">
                <input type="text" name="search" value="@Model.SearchTerm" class="form-control" placeholder="جستجو در دسته‌بندی‌ها..." />
                <button class="btn btn-outline-primary" type="submit"><i class="bi bi-search"></i></button>
            </div>
        </div>
    </div>
</form>

<!-- Categories Table -->
<div class="card border-0 shadow-sm rounded-3">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="width:50px">#</th>
                        <th>نام دسته‌بندی</th>
                        <th>تعداد محصولات</th>
                        <th>تاریخ ایجاد</th>
                        <th style="width:150px">عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Categories.Any())
                    {
                        int row = 1;
                        foreach (var item in Model.Categories)
                        {
                            <tr id="row-@item.Id">
                                <td>@row</td>
                                <td>@item.Name</td>
                                <td><span class="badge bg-info">@item.ProductCount</span></td>
                                <td>@item.PersianCreateDate</td>
                                <td>
                                    <button class="btn btn-sm btn-outline-warning" onclick="editCategory(@item.Id, '@item.Name')" title="ویرایش">
                                        <i class="bi bi-pencil"></i>
                                    </button>
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline" onsubmit="return confirm('آیا از حذف این دسته‌بندی مطمئن هستید؟')">
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="حذف">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    }
                                </td>
                            </tr>
                            row++;
                        }
                    }
                    else
                    {
                        <tr><td colspan="5" class="text-center text-muted py-4">دسته‌بندی‌ای یافت نشد</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Create Modal -->
<div class="modal fade" id="createModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-action="Create" method="post">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title"><i class="bi bi-plus-circle"></i> دسته‌بندی جدید</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">نام دسته‌بندی</label>
                        <input type="text" name="Name" class="form-control" required maxlength="100" />
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">انصراف</button>
                    <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg"></i> ثبت</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-action="Edit" method="post">
                <div class="modal-header bg-warning text-dark">
                    <h5 class="modal-title"><i class="bi bi-pencil"></i> ویرایش دسته‌بندی</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" name="Id" id="editId" />
                    <div class="mb-3">
                        <label class="form-label fw-bold">نام دسته‌بندی</label>
                        <input type="text" name="Name" id="editName" class="form-control" required maxlength="100" />
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">انصراف</button>
                    <button type="submit" class="btn btn-warning"><i class="bi bi-check-lg"></i> ویرایش</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
    function editCategory(id, name) {
        $('#editId').val(id);
        $('#editName').val(name);
        new bootstrap.Modal($('#editModal')).show();
    }
</script>
}
