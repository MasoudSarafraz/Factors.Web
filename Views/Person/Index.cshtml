@model Factors.Web.Models.ViewModels.PersonListViewModel
@{
    ViewData["Title"] = "اشخاص";
}

<div class="page-header">
    <h4><span class="icon-badge icon-badge-teal"><i class="bi bi-people"></i></span> اشخاص</h4>
    <button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#createModal">
        <i class="bi bi-plus-lg"></i> شخص جدید
    </button>
</div>

<!-- Search -->
<div class="search-bar">
    <form asp-action="Index" method="get">
        <div class="row g-2 align-items-center">
            <div class="col-md-6">
                <div class="input-group input-group-sm">
                    <input type="text" name="search" value="@Model.SearchTerm" class="form-control form-control-sm" placeholder="جستجو در اشخاص..." />
                    <button class="btn btn-outline-primary btn-sm" type="submit"><i class="bi bi-search"></i></button>
                </div>
            </div>
        </div>
    </form>
</div>

<!-- Persons Table -->
<div class="card border-0 shadow-sm rounded-3">
    <div class="card-header-compact">
        <span class="icon-badge icon-badge-teal"><i class="bi bi-people"></i></span>
        <span class="header-title">لیست اشخاص</span>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0 table-compact">
                <thead class="table-light">
                    <tr>
                        <th style="width:50px">#</th>
                        <th>نام</th>
                        <th>نوع</th>
                        <th>تعداد فاکتور</th>
                        <th>تاریخ ایجاد</th>
                        <th style="width:150px">عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Persons.Any())
                    {
                        int row = 1;
                        foreach (var item in Model.Persons)
                        {
                            <tr>
                                <td>@row</td>
                                <td>@item.PersonName</td>
                                <td>
                                    @if (item.IsIndividual)
                                    {
                                        <span class="badge bg-info">حقیقی</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning text-dark">حقوقی</span>
                                    }
                                </td>
                                <td><span class="badge bg-primary">@item.FactorCount</span></td>
                                <td>@item.PersianCreateDate</td>
                                <td>
                                    <button class="btn btn-sm btn-outline-warning" onclick="editPerson(@item.Id)" title="ویرایش">
                                        <i class="bi bi-pencil"></i>
                                    </button>
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline" onsubmit="return confirm('آیا از حذف این شخص مطمئن هستید؟')">
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="حذف"><i class="bi bi-trash"></i></button>
                                        </form>
                                    }
                                </td>
                            </tr>
                            row++;
                        }
                    }
                    else
                    {
                        <tr><td colspan="6" class="text-center text-muted py-4">شخصی یافت نشد</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Create Modal -->
<div class="modal fade" id="createModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-action="Create" method="post">
                <div class="modal-header-compact">
                    <h5 class="modal-title"><span class="icon-badge icon-badge-sm icon-badge-teal"><i class="bi bi-plus-circle"></i></span> شخص جدید</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body-compact">
                    <div class="mb-2">
                        <label class="form-label-compact">نام</label>
                        <input type="text" name="PersonName" class="form-control form-control-sm" required maxlength="150" />
                    </div>
                    <div class="mb-2">
                        <label class="form-label-compact">نوع شخص</label>
                        <select name="IsIndividual" class="form-select form-select-sm">
                            <option value="true">حقیقی</option>
                            <option value="false">حقوقی</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer-compact">
                    <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">انصراف</button>
                    <button type="submit" class="btn btn-primary btn-sm"><i class="bi bi-check-lg"></i> ثبت</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-action="Edit" method="post">
                <div class="modal-header-compact">
                    <h5 class="modal-title"><span class="icon-badge icon-badge-sm icon-badge-orange"><i class="bi bi-pencil"></i></span> ویرایش شخص</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body-compact">
                    <input type="hidden" name="Id" id="editId" />
                    <div class="mb-2">
                        <label class="form-label-compact">نام</label>
                        <input type="text" name="PersonName" id="editPersonName" class="form-control form-control-sm" required maxlength="150" />
                    </div>
                    <div class="mb-2">
                        <label class="form-label-compact">نوع شخص</label>
                        <select name="IsIndividual" id="editIsIndividual" class="form-select form-select-sm">
                            <option value="true">حقیقی</option>
                            <option value="false">حقوقی</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer-compact">
                    <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">انصراف</button>
                    <button type="submit" class="btn btn-warning btn-sm"><i class="bi bi-check-lg"></i> ویرایش</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
    function editPerson(id) {
        $.get('/Person/GetById/' + id, function(data) {
            $('#editId').val(data.id);
            $('#editPersonName').val(data.personName);
            $('#editIsIndividual').val(data.isIndividual.toString());
            new bootstrap.Modal($('#editModal')).show();
        });
    }
</script>
}
